# server-client-RPC
RPC сервер вычисляет расстояние между двумя точками на плоскости и передает ответ клиенту который выводит ответ в консоль
**************

Запуск программы: в корневом каталоге проекта выполнить команды `docker-compose build` `docker-compose up`
